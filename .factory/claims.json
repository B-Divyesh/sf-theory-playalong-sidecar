[
  {
    "id": "harmony-context",
    "claim": "Shows whether each played note is in the selected key and which matching chords include it.",
    "where": "landing preview, demo workspace, README",
    "test": "npm test -- --grep @claim:harmony-context",
    "sandbox": "fresh browser context at /demo; replay E after changing C major to E minor and assert note, scale, and chords change"
  },
  {
    "id": "midi-input",
    "claim": "Reads notes played on a connected MIDI keyboard.",
    "where": "landing copy, MIDI control, README",
    "test": "npm test -- --grep @claim:midi-input",
    "sandbox": "fresh browser context at /demo with a synthetic Web MIDI input"
  },
  {
    "id": "local-audio",
    "claim": "Keeps local audio, MIDI messages, and demo notes on this device; demo changes are not saved as real practice data.",
    "where": "landing facts, demo banner, privacy page, README",
    "test": "npm test -- --grep @claim:local-audio",
    "sandbox": "fresh browser context at /demo; load and play a valid local WAV, send synthetic MIDI, record requests, and inspect storage"
  },
  {
    "id": "beat-marker",
    "claim": "Shows an eight-beat marker that advances with audio playback at the chosen tempo.",
    "where": "landing workspace, demo workspace, README",
    "test": "npm test -- --grep @claim:beat-marker",
    "sandbox": "fresh demo runs at 60 and 120 beats per minute; measure the first beat interval against 60 / tempo"
  },
  {
    "id": "csv-export",
    "claim": "Exports note history as CSV.",
    "where": "history toolbar, privacy page, README",
    "test": "npm test -- --grep @claim:csv-export",
    "sandbox": "fresh browser context at /demo; compare downloaded CSV rows with sample history"
  },
  {
    "id": "history-portability",
    "claim": "Exports and imports note history as a backup file.",
    "where": "history toolbar, README",
    "test": "npm test -- --grep @claim:history-portability",
    "sandbox": "fresh browser context at /demo; export the four sample notes and import that backup"
  },
  {
    "id": "local-history",
    "claim": "Keeps key, scale, tempo, and note history in this browser but does not store audio files.",
    "where": "landing privacy section, privacy page, README",
    "test": "npm test -- --grep @claim:local-history",
    "sandbox": "fresh context at /; change key, scale, and tempo; play a note; choose local audio; reload and inspect every state"
  },
  {
    "id": "offline-reload",
    "claim": "Works offline after the first visit.",
    "where": "landing facts, README",
    "test": "npm test -- --grep @claim:offline-reload",
    "sandbox": "fresh browser context at /demo; wait for service worker, set context offline, reload and play C"
  },
  {
    "id": "free-use",
    "claim": "Free to use with no account or payment gate.",
    "where": "landing facts, privacy page, terms page, README",
    "test": "npm test -- --grep @claim:free-use",
    "sandbox": "fresh browser context; enter demo from landing and assert no credential or payment gate"
  },
  {
    "id": "keyboard-fallback",
    "claim": "The screen keys and computer keys A–K work without MIDI hardware.",
    "where": "workspace help, demo intro, README",
    "test": "npm test -- --grep @claim:keyboard-fallback",
    "sandbox": "fresh 390px browser context at /demo; click a screen key and press a mapped computer key without Web MIDI"
  },
  {
    "id": "demo-ready",
    "claim": "The demo opens with a playable C-major groove and four notes in its note history.",
    "where": "landing demo action, demo intro, README",
    "test": "npm test -- --grep @claim:demo-ready",
    "sandbox": "fresh browser context at /?demo=1; start the sample and assert playback advances, remains unpaused, and shows exactly C, E, F-sharp, and G"
  },
  {
    "id": "playalong-continuity",
    "claim": "Screen, computer, and MIDI notes work while the sample audio continues playing.",
    "where": "landing how-it-works, README",
    "test": "npm test -- --grep @claim:playalong-continuity",
    "sandbox": "fresh browser context at /demo; start sample audio, use all three input paths, and assert audio time keeps increasing"
  },
  {
    "id": "no-third-party-requests",
    "claim": "Makes no third-party requests during real or demo practice; audio files, MIDI messages, and note history are not uploaded.",
    "where": "privacy page, README",
    "test": "npm test -- --grep @claim:no-third-party-requests",
    "sandbox": "fresh browser context; record cold load, real and demo notes, local audio playback, synthetic MIDI, exports, and reloads; allow only same-origin GET or local blob requests"
  },
  {
    "id": "history-deletion",
    "claim": "Clear history removes note history from this browser.",
    "where": "privacy page, note history controls",
    "test": "npm test -- --grep @claim:history-deletion",
    "sandbox": "fresh real-mode context; save settings and notes, confirm Clear history, reload, and assert note history stays empty while settings remain"
  }
]
